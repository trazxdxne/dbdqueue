# Dead By Queue (`dbdq`)

[![GitHub Release](https://img.shields.io/github/v/release/trazxdxne/dbdqueue?style=flat-square&color=f38ba8)](https://github.com/trazxdxne/dbdqueue/releases)
[![CI Status](https://img.shields.io/github/actions/workflow/status/trazxdxne/dbdqueue/ci.yml?branch=master&style=flat-square&label=ci)](https://github.com/trazxdxne/dbdqueue/actions)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg?style=flat-square)](LICENSE)
[![Rust Edition](https://img.shields.io/badge/Rust-2024%20edition-dea584?style=flat-square&logo=rust)](https://www.rust-lang.org/)
[![Platform](https://img.shields.io/badge/Platform-Linux%20%7C%20Windows-lightgrey?style=flat-square)](https://github.com/trazxdxne/dbdqueue/releases)

<p align="center">
  <b>Language / Язык:</b>&nbsp;
  <a href="README.md"><kbd>English</kbd></a>&nbsp;•&nbsp;<kbd><b>Русский</b></kbd>
</p>

> [!IMPORTANT]
> **Юридическое уведомление и отказ от ответственности**
> Dead By Queue — независимая утилита с открытым исходным кодом, разработанная сообществом и использующая публичный API `deadbyqueue.com`. Проект **не связан, не поддерживается, не спонсируется и не одобрен** Behaviour Interactive Inc., Amazon Web Services, Inc. (AWS) или `deadbyqueue.com`.
> *"Dead by Daylight"* является зарегистрированным товарным знаком Behaviour Interactive Inc. *"AWS"* и сопутствующие знаки являются товарными знаками Amazon.com, Inc. или её дочерних компаний. Все региональные параметры маршрутизации изменяют исключительно локальное разрешение сетевых адресов в операционной системе. См. [LICENSE](LICENSE) для ознакомления с полными юридическими условиями и ограничением ответственности.

---

Быстрый нативный терминальный дашборд (TUI) с нулевыми внешними зависимостями для игроков Dead by Daylight. Dead By Queue отображает актуальное время ожидания в очередях подбора матчей и задержку до серверов (пинг) во всех глобальных дата-центрах AWS. Приложение оснащено умной рекомендательной системой с учётом выбранной роли и встроенным механизмом блокировки нежелательных регионов для избавления от высокого пинга.

<p align="center">
  <img src="assets/dashboard.svg" alt="Терминальный дашборд Dead By Queue" width="100%">
</p>

---

## Возможности

### Интерактивный терминальный интерфейс на Ratatui
- **Максимальная производительность**: Полностью написано на Rust с использованием `ratatui` и `crossterm` — никакого Node.js, Electron или других тяжеловесных сред выполнения.
- **Адаптивный интерфейс**: Автоматическое центрирование таблиц и гарантированное отображение панели **«Лучший выбор»**, шапки и панели клавиш на стандартных терминалах $80 \times 24$ (и высотой $\ge 14$) с плавной прокруткой строк.
- **Наглядный формат времени**: Время ожидания отображается как `Xs` ($<60$ с), `m:ss` (от `1:00` до `59:59`) и `1h+` / `2h+` ($\ge 1$ ч), предотвращая смещение колонок и облегчая быстрое чтение.
- **Точное время обновления**: Отображает реальный возраст данных, полученных с API, а не просто время локального запроса.

### Рекомендательная система с учётом роли («Лучший выбор»)
Вместо банального выбора наименьшего времени ожидания, `dbdq` рассчитывает комбинированный рейтинг с учётом роли, балансируя между временем поиска матча и задержкой сети:

$$\text{Счёт} = \text{Время ожидания (сек)} + \text{Штраф за пинг}(\text{Пинг}, \text{Роль})$$

- **Штрафы за пинг по ролям**:
  - **Маньяк** (авторитетная валидация попаданий на стороне хоста): Штрафы плавно растут при задержке выше $70\text{ мс}$ и резко возрастают только после $140\text{ мс}$.
  - **Выживший** (критически важна быстрая реакция на скиллчеки и быстрые перепрыгивания): Штрафы начисляются уже с $60\text{ мс}$, значительно усиливаясь свыше $90\text{ мс}$.
- **Индикатор `+N похожих`**: Указывает, когда альтернативные регионы предлагают сравнимое качество подбора (в пределах $\pm 25\text{ мс}$ по задержке и $\pm 15\text{ с}$ по времени ожидания).
- **Корректность данных**: Регионы с неизмеренным пингом или отключенные игровые серверы автоматически исключаются из рекомендаций.

### Блокировка регионов подбора матчей AWS
- **Локальная фильтрация маршрутизации**: Добавляйте нужные регионы серверов в белый список (`l` в TUI или `dbdq lock [регионы]`), блокируя отдалённые регионы с высоким пингом.
- **Интерактивное консольное меню**: Команда `dbdq lock` запускает полнофункциональное меню на базе Ratatui в альтернативном экранном буфере с плавной прокруткой и полным отсутствием мерцания, оставляя историю консоли чистой после выхода.
- **Без влияния на игру**: Работает исключительно через системную маршрутизацию `hosts` на уровне ОС — без перехвата пакетов, модификации памяти или использования сторонних VPN.
- **Неблокирующее повышение привилегий**: Запрос прав администратора (UAC / sudo) выполняется вне главного цикла событий, поэтому интерфейс остаётся отзывчивым.

### Фоновое неблокирующее обновление
- **Параллельные сетевые потоки**: Фоновые потоки опрашивают очереди и измеряют 1-RTT HTTP/1.1 Keep-Alive пинг без зависания интерфейса.
- **Наглядная анимация**: Анимированный спиннер Брайля (`⠋ Обновление...`) в строке состояния во время опроса и автоугасающие уведомления (`[✓ Обновлено]`).

### Поддержка любых раскладок клавиатуры
- Горячие клавиши полноценно работают при любой активной раскладке клавиатуры. Все действия моментально срабатывают как на латинице, так и на кириллице (русской раскладке) без необходимости переключать язык ввода.

### Сохранение настроек на двух платформах
- Настройки (сортировка, режим игры, заблокированные регионы, язык интерфейса) автоматически сохраняются между запусками:
  - **Linux**: `~/.config/dbdqueue/config.toml`
  - **Windows**: `%APPDATA%\dbdqueue\config.toml`

---

## Установка и быстрый старт

### Windows (PowerShell)
Установка и запуск `dbdq` одной командой:
```powershell
irm https://raw.githubusercontent.com/trazxdxne/dbdqueue/master/install.ps1 | iex
```
*Загружает последний бинарный релиз в `%LOCALAPPDATA%\Programs\dbdqueue`, добавляет путь в пользовательскую переменную `PATH` и запускает дашборд.*

### Linux (Bash)
Установка и запуск `dbdq` одной командой:
```bash
curl -fsSL https://raw.githubusercontent.com/trazxdxne/dbdqueue/master/install.sh | bash
```
*Определяет архитектуру системы (`x86_64` или `aarch64`), устанавливает бинарный файл в `/usr/local/bin` (или `~/.local/bin`) и запускает дашборд.*

---

## Готовые сборки и архивы

Архивы релизов и автономные исполняемые файлы доступны на странице [GitHub Releases](https://github.com/trazxdxne/dbdqueue/releases):

| Платформа | Архитектура | Архив релиза | Исполняемый файл |
| :--- | :--- | :--- | :--- |
| **Linux** | `x86_64` | `dbdq-linux-x86_64.tar.gz` | `dbdq-linux-x86_64` |
| **Linux** | `aarch64` | `dbdq-linux-aarch64.tar.gz` | `dbdq-linux-aarch64` |
| **Windows** | `x64` | `dbdq-windows-x64.zip` | `dbdq-windows-x64.exe` |

### Проверка контрольных сумм
Каждый релиз содержит официальный файл `SHA256SUMS.txt` с криптографическими контрольными суммами всех файлов. Для проверки скачанного дистрибутива:

**Linux / macOS:**
```bash
sha256sum -c SHA256SUMS.txt --ignore-missing
```

**Windows (PowerShell):**
```powershell
Get-FileHash -Algorithm SHA256 dbdq-windows-x64.zip
```

---

## Сборка из исходного кода

Убедитесь, что у вас установлена последняя стабильная версия компилятора Rust (`rustup update`).

```bash
# Клонировать репозиторий
git clone https://github.com/trazxdxne/dbdqueue.git
cd dbdqueue

# Собрать оптимизированный релизный бинарный файл
cargo build --release

# Скомпилированный файл будет находиться по пути:
# Linux:   target/release/dbdq
# Windows: target\release\dbdq.exe
```

Либо установите напрямую в директорию Cargo bin:
```bash
cargo install --path .
```

---

## Использование

Запуск интерактивного дашборда:
```bash
dbdq
```

### Горячие клавиши

| Клавиша | Действие |
| :--- | :--- |
| `↑` / `↓` | Выбор / прокрутка строк таблицы |
| `l` | Открыть меню **Блокировка регионов** |
| `s` | Переключить сортировку (`Маньяк` → `Выживший` → `Пинг`) |
| `m` | Переключить режим игры (`Обычный` ↔ `Ивент`) |
| `r` | Запустить немедленное обновление данных и пинга |
| `Esc` | Закрыть модальное окно или выйти из приложения |

### Команды и параметры CLI

Вы можете использовать `dbdq` без графического TUI или задавать начальные параметры прямо из командной строки:

```bash
# Запуск с заданной сортировкой
dbdq --sort ping
dbdq --sort killer
dbdq --sort survivor

# Фильтрация по режиму подбора
dbdq --mode standard
dbdq --mode event

# Заблокировать регионы AWS (разрешить только указанные, заблокировав остальные)
dbdq lock frankfurt dublin

# Интерактивное меню блокировки регионов в CLI
dbdq lock

# Сбросить все блокировки регионов и вернуть стандартный подбор
dbdq unlock
```

---

## Конфигурация

Параметры сохраняются в формате TOML:

```toml
mode = "Standard"       # "Standard" (Обычный) или "Event" (Ивент)
sort = "Killer"         # "Killer" (Маньяк), "Survivor" (Выживший), "Ping" (Пинг) или "Default" (По умолчанию)
locked = ["eu-central-1"] # Список разрешённых кодов регионов AWS
lang = "auto"           # "auto" (автоопределение), "en" (английский) или "ru" (русский)
```

- **Linux**: `~/.config/dbdqueue/config.toml`
- **Windows**: `%APPDATA%\dbdqueue\config.toml`

Также поддерживаются переменные окружения:
- `DBD_API_URL`: Переопределить адрес API собственным зеркалом.
- `HTTP_PROXY` / `HTTPS_PROXY`: Стандартная маршрутизация через HTTP/HTTPS-прокси.

---

## Технические характеристики

- **Язык разработки**: 100% Rust (Редакция 2024)
- **TUI-фреймворк**: `ratatui` (v0.30+) и `crossterm` (v0.27+)
- **HTTP-клиент**: `ureq` (v2.9+) с пулом TLS-соединений
- **Парсер CLI**: `clap` (v4.4+) с derive-макросами
- **Сериализация**: `serde`, `serde_json`, `toml`
- **Поддерживаемые ОС**: Linux (x86_64, aarch64), Windows (x64)

---

<p align="center">
  <a href="README.md">Switch to English version (README.md)</a>
</p>
